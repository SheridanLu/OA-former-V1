version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: mochu-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: mochu@2026
      MYSQL_DATABASE: mochu_oa
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
      - ./docs/sql/V3.2_schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
    command: --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_general_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-pmochu@2026"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 30s

  redis:
    image: redis:7-alpine
    container_name: mochu-redis
    restart: unless-stopped
    command: redis-server --requirepass mochu@2026
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "mochu@2026", "ping"]
      interval: 3s
      timeout: 3s
      retries: 10
      start_period: 5s

  minio:
    image: minio/minio:RELEASE.2024-06-13T22-53-53Z
    container_name: mochu-minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: mochu_admin
      MINIO_ROOT_PASSWORD: mochu@2026
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
    command: server /data --console-address ":9001"

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: mochu-backend
    ports:
      - "9090:9090"
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_started
    restart: on-failure
    healthcheck:
      test: ["CMD-SHELL", "curl -s -o /dev/null -w '%{http_code}' -X POST -H 'Content-Type: application/json' -d '{\"username\":\"ping\"}' http://localhost:9090/api/v1/auth/check-account | grep -qE '^[2-4]'"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 60s
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/mochu_oa?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: mochu@2026
      SPRING_DATA_REDIS_HOST: redis
      SPRING_DATA_REDIS_PASSWORD: mochu@2026
      MINIO_ENDPOINT: http://minio:9000

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    container_name: mochu-frontend
    ports:
      - "80:80"
    depends_on:
      backend:
        condition: service_healthy
    restart: on-failure

volumes:
  mysql-data:
  redis-data:
  minio-data:
